<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.poul.bloodykeras">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.NFC" />
    <uses-feature android:name="android.hardware.nfc" android:required="true" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PatientScreen" />
        <activity android:name=".keratiatikachoices" />
        <activity android:name=".DonorProperties" />
        <activity android:name=".Session" />
        <activity android:name=".DonorSearch" />
        <activity android:name=".PatientSearch" />
        <activity android:name=".NewBag" />
        <activity android:name=".ApplicationScreen" />
        <activity android:name=".UpdateBag" />
        <activity
            android:name=".NDchoices"
            android:label="@string/title_activity_ndchoices"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".PieChartScreen"></activity>
    </application>

</manifest>
